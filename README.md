# grunt-doc-merge

> Run elements through doc-merge

## Example usage

```
doc_merge: {
default: {
  options: {
    merge: true,
    config: 'test/fixtures/config.json'
  },
  src: 'test/fixtures',
  dest: 'test/tmp'
}
```
